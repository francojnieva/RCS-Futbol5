// require('dotenv').config(); // Asegúrate de que esto esté al principio del archivo
// const mongoose = require('mongoose');

// const mongoURI = process.env.MONGODB; // Obtén la URI de las variables de entorno

// if (!mongoURI) {
//   throw new Error('MONGODB URI no está definida en las variables de entorno');
// }

// mongoose.connect(mongoURI)
//   .then(() => console.log('db conectada'))
//   .catch((error) => console.error('Error al conectar a MongoDB:', error));

// module.exports = mongoose;
